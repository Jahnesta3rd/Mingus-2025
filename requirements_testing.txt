# MINGUS Comprehensive Testing Suite Requirements
# Install with: pip install -r requirements_testing.txt

# Core testing dependencies
requests>=2.28.0
dataclasses>=0.6; python_version < "3.7"

# Accessibility testing tools
# axe-core (JavaScript library - install via npm)
# WAVE API (web service)
# Lighthouse (install via npm: npm install -g lighthouse)

# Performance testing
# Chrome DevTools Protocol (for real performance metrics)

# Mobile testing
# BrowserStack or similar for real device testing

# Optional: Additional testing tools
pytest>=7.0.0
pytest-html>=3.1.0
pytest-cov>=4.0.0

# For CI/CD integration
pytest-xdist>=2.5.0
pytest-html>=3.1.0

# For reporting
jinja2>=3.1.0
markdown>=3.4.0

# For network simulation
# mitmproxy (optional, for network condition simulation)

# For visual testing
# percy (optional, for visual regression testing)

# For load testing
# locust (optional, for performance load testing)
